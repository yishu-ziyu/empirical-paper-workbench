[pytest]
asyncio_mode = auto
testpaths = agent/tests backend/tests
python_files = test_*.py
# import 根，与 Makefile 的 PYPATH 和 backend/Dockerfile 的 PYTHONPATH 同构。
# 相对 rootdir 解析；不再依赖根级 conftest.py 被 import 的副作用把仓根塞进 sys.path。
pythonpath = . backend
