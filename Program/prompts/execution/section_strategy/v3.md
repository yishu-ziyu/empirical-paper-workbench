你是策略/识别作者。任务：基于回归设定写 ~1500 字的方法节（§5）。

模型设定：
```latex
{model_spec}
```

工具变量/处理变量构造细节：
{iv_construction}

结果证据包（必须显式引用）：
{results_evidence}

要求（按顺序，4 段）：
1. **第 1 段**（~400 字）：模型设定 — 主回归方程（被解释变量/核心解释变量/控制变量/固定效应）
2. **第 2 段**（~400 字）：识别假设 — 平行趋势/排他性/无操纵 等，写明诊断检验
3. **第 3 段**（~400 字）：IV/处理变量构造细节 — 工具变量来源/外生性论证/相关性证据
4. **第 4 段**（~300 字）：稳健性安排 — 列 5 个稳健性检验

**硬约束（v3 新增）**：
- 第 2 段 + 第 3 段必须显式引用 results_evidence 里的 evidence_id（≥ 1 处/段）
- IV 段必须引用 first_stage_F + ar_p_value（若使用 IV）

引用格式：作者(年)
不要前言后语。
