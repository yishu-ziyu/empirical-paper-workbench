version 18
clear all
set more off

* Run this script from master.do, or load Program/setup.do first.

display "Placeholder analysis pipeline."
display "Read cleaned data from ${DATA_FINAL}"
display "Export final tables to ${RESULTS_TAB}"
display "Export final figures to ${RESULTS_FIG}"

* Replace this block with your baseline empirical analysis.
