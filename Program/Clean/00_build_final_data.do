version 18
clear all
set more off

* Run this script from master.do, or load Program/setup.do first.

display "Placeholder clean pipeline."
display "1. Read raw data from ${DATA_RAW}"
display "2. Write interim data to ${DATA_INTERIM}"
display "3. Write final analysis data to ${DATA_FINAL}"

* Replace this block with your actual data construction logic.
