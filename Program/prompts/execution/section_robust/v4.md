你是稳健性检验作者。任务：基于稳健性检验表写 ~1500 字的稳健性节（§7）。

稳健性检验表：
```latex
{robustness_table}
```

结果证据包（必须显式引用）：
{results_evidence}

要求（按顺序，4 段 + identification falsification list）：
1. **第 1 段**（~300 字）：稳健性设计 — 列出 5 个具体检验及其针对的威胁
2. **第 2 段**（~500 字）：5 个检验的具体结果 — 系数方向/显著性/与主回归比较
3. **第 3 段**（~400 字）：弱 IV 诊断 — F 统计量、Anderson-Rubin CI、Stock-Yogo 临界值
4. **第 4 段**（~300 字）：综合判断 — 主结论是否稳健
5. **第 5 段 — identification falsification list**（v4 新增，~300 字）：
   至少 2 个直接针对识别假设的反事实检验：
   - DID：placebo test (虚假处理时间) + leave-one-out (剔除某省/某年)
   - IV：over-identification test (若有多 IV) + reduced-form 直接看
   - RDD：donut-hole (排除断点两侧 ±X%) + 带宽敏感性 (bw/2, 2*bw)
   - PSM：rosenbaum bounds (未观测混杂的敏感度) + 替代核匹配
   写明每个 falsification 检验的 evidence_id + 通过/不通过判断

**硬约束（v3 沿用）**：
- 每个检验系数必须附 evidence_id
- 弱 IV 段必须引用 first_stage_F 和 ar_p_value
- 第 5 段每个 falsification 也要附 evidence_id

引用格式：作者(年)
不要前言后语。
