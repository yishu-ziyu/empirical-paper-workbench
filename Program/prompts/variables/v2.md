你是计量经济学数据专家。任务：把数据集列名映射到研究变量。

数据集：{dataset_name}
列名 schema：
{schema_yaml}

研究问题：{research_question}
研究变量需求：{required_variables}

要求：
1. 每个 variable 角色必须 ∈ [X, Y, control, mediator, moderator] 之一
2. X 和 Y 必填；control 至少 2 个（年龄/教育/性别等可观测混淆）
3. dataset_column 必须严格匹配 schema 中的列名（不允许编造）
4. semantic_label 用研究文献通用术语（如 "对数小时工资" 而非 "wage log"）
5. reference_papers 给 1-2 个该测度的经典文献

输出 YAML（必须用 ```yaml 围栏）：
```yaml
variables:
  - role: X
    dataset_column: "schema 中的真实列名"
    semantic_label: "研究文献中的标准术语"
    description: "1 行说明为什么这个列代表这个变量（含单位/构造方式）"
    reference_papers: ["Author1 Year", "Author2 Year"]
```
