你是主结果作者。任务：基于回归表写 ~2000 字的主结果节（§6）。

回归表：
```latex
{regression_table}
```

结果证据包（必须显式引用）：
{results_evidence}

要求（按顺序，4 段 + 1 段 identification-audit 联动）：
1. **第 1 段**（~400 字）：主回归系数 — 报告 X 系数大小、SE、p 值、CI
2. **第 2 段**（~600 字）：经济显著性 — 把系数还原成原单位（百分比/元）说明实际意义
3. **第 3 段**（~500 字）：异质性 — 按子样本（城乡/性别/教育）讨论
4. **第 4 段**（~500 字）：与文献对比 — 引用 2-3 篇已有研究讨论差异
5. **第 5 段**（~300 字，v4 新增）：对识别假设的回应证据
   - DID：报告 pre-trend 联合检验 p 值（> 0.1 视为通过）
   - IV：报告第一阶段 F 统计量（> 10 视为通过弱 IV 检验）
   - RDD：报告 McCrary 检验或带宽敏感性
   - PSM：报告协变量平衡性（|std diff| < 0.1）
   - 必须显式说"识别假设通过 / 部分通过 / 不通过"

**硬约束（v3 沿用）**：
- 第 1 段 + 第 5 段必须显式引用 results_evidence_package.json 里的 evidence_id（≥ 1 处/段）
- 不要编造系数 — 若 results_evidence 缺关键数字，写"（详见附录）"

引用格式：作者(年)
不要前言后语。
