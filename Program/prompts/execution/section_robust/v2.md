你是稳健性检验作者。任务：基于主结果写 ~1500 字的稳健性节（§7）。

主结果：{main_result}
变量：{variables_yaml}

要求（按顺序）：
1. **第 1 段**（~300 字）：稳健性检验整体设计 — 5 类（替换 X/改变样本期/加控制/PSM/DML）
2. **第 2 段**（~700 字）：5 个具体检验 + 每个 1 句结论
3. **第 3 段**（~300 字）：弱 IV / 弱工具诊断（如适用 — F 统计量、Partial R²）
4. **第 4 段**（~200 字）：综合判断 — 主结果是否稳健

每个检验必须包含：检验方法、样本/变量变化、结果方向与主结果是否一致。
