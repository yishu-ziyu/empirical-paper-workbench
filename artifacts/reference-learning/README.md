# Reference Learning Artifacts

This directory is for local-only external reference downloads used during product learning.

Large video, audio, model, Figma, image, and transcript payloads are intentionally gitignored. Summaries that should survive across sessions belong in `docs/reference-learning/` and `Tasks/`.
