# UX / 产品走查 Backlog

> 2026-08-28 · ZCode 内置浏览器全流程走查（真实用户路径：注册 → 登录 → 示例数据 → 研究方向 → 估计 → 大纲 → 第一章 → 批准 → 导出 → 退出）
> 环境：auth-upgrade 分支 · LLM 降级路径（未设 key）· 1280×720
> 用途：后续迭代依据。每条含现象与建议，修完请回填状态。

## Bug（功能缺陷）

| # | 级别 | 现象 | 建议 | 状态 |
|---|---|---|---|---|
| B1 | P0 | 登录/注册成功后停留在登录/注册页，用户以为没成功 | `handleLogin` 成功时同时 `setAuthPage(null)` | ✅ 已随认证 PR 修复 |
| B2 | P1 | 已登录用户回到落地页，头部仍显示「登录/注册」——UnauthHeader 不感知登录态 | 落地页头部按 authed 切换为「进入工作台 / 退出」 | ✅ 已修（authed 显示 进入工作台/退出） |
| B3 | P1 | 退出登录后落在"空桌"中间态：既非落地页也非引导页，无明确返回入口 | 退出后回落地页（或登录页），并清 `deskOpen/seenGuide` | ✅ 已修 |

## UX / 交互

| # | 级别 | 现象 | 建议 |
|---|---|---|---|
| U1 | P1 | 中间状态卡片不收起：方向确认卡（「是，再补充/否，进入大纲」）在大纲已出、第一章已写完后仍渲染在页面中部，页面成为"状态地层"，越走越长 | 已完成的 HITL 节点折叠为一行摘要（如 ✓ 方向已确认 · 展开），或仅保留最新暂停卡 | ✅ 已修（进入大纲阶段即折叠为一行，可展开） |
| U2 | P1 | 大纲确认拆成两步：先「通过大纲」再进入可编辑大纲、还要「应用并生成」 | 合并为一步：可编辑预览 + 单个确认按钮 | ✅ 已修（批准大纲并入配置卡为主按钮，出大纲降为次按钮） |
| U3 | P1 | 批准词汇三套并存：工具条「通过」、评审面板「接受/拒绝重生成/强制通过」、提示文案里"写下一章" | 统一动词（建议全部用「批准」），主按钮每屏唯一 | ✅ 已修（批准本章/批准并继续/打回重写/强制批准） |
| U4 | P2 | 顶部"下一步"提示跳步：第 1/6 章刚批准就提示"润色后导出" | 提示接真实进度（"第 1 章已批准，下一章：文献综述"） | ✅ 已修（进度提示 第X/Y章） |
| U5 | P2 | HITL 暂停问答文案生硬："是，再补充 / 否，进入第 1 部分大纲"语义不明 | 明确选项文案，如「补充研究信息 / 就用这些，出大纲」 | ✅ 已修 |
| U6 | P2 | 评审面板「自动通过 第 0.2 轮 · 综合 0.88 source=llm」密度高不可读：轮次、阈值、来源无解释 | 加一句人话（"AI 评审 0.88 分 ≥ 0.8 阈值，自动通过"）+ 阈值可配说明 | ✅ 已修（增加人话判决行） |
| U7 | P2 | "已收集研究信息"确认卡与表单信息完全重复 | 默认折叠一行，点开看详情 | ✅ 已修（与 U1 同一折叠实现） |
| U8 | P2 | 导出按钮 disabled 无原因提示 | tooltip/副文案："写完第一章后解锁" | ✅ 已修（title 提示） |
| U9 | P3 | 主表仅显示 age 一行，treat 在控制变量里不出现 | 主表列出全部模型变量，或注明"仅显示关键变量" | ✅ 已修（表题注明） |
| U10 | P3 | 落地页信息冗长：hero + 四步 + 样例论文 + 工作台预览 + 功能 + CTA，「用示例数据」同按钮出现 3 次 | 收敛为一屏 + 锚点导航，CTA 唯一 | ⏸ 缓办——落地页改版需设计定稿，避免主观重排 |
| U11 | P3 | 表单术语「因变量/自变量/控制变量」对新手有门槛，且界面 Y income · X age 混排 | 统一术语或加悬停解释（结局/暴露/混杂） | ✅ 已修（标签悬停解释） |
| U12 | P3 | 登录/注册页 banner 无返回首页入口 | 点 logo 回首页 | ✅ 已修 |

## 视觉

| # | 级别 | 现象 | 建议 |
|---|---|---|---|
| D1 | P2 | 主 CTA 色板不统一：注册页按钮绿色系，落地页/登录页黑色系 | 统一 accent 变量，全站单一主色 | ✅ 已修（落地/认证主 CTA 统一 ink 黑；工作台保留 accent） |

## 待观察 / 环境限制

| # | 说明 |
|---|---|
| T1 | IAB（内置浏览器）内文件下载未验证（浏览器能力限制），.do/.docx 实际下载需在真实浏览器复核 |
| T2 | 无 LLM key 时全流程可走通（降级路径质量可接受），正式 demo 前接 key 复核语言层 |

## 走查中的正面记录（保持，不要迭代掉）

- 估计读数卡（主张/识别星级/文献来源/稳健性）信息架构清晰，"相关不读成因果"表达准确
- 右栏锁定路径 + 步骤点亮反馈直观，运行档案（upload_pipeline ok 95ms）增加可信度
- 导出代码对话框干净：4 格式带一行说明
- 降级 LLM 的章节文案质量可用（引言约 500 字，结构完整）
- 认证链路实测可靠：httpOnly cookie + 退出后 /auth/me 即刻 401（服务端真吊销）

## 停放的实验性方向

- [Research Canvas：将任意材料展开为研究关系图](notes/research-canvas-experiment-2026-08-29.md)——仅留档，当前不开发；产品继续聚焦现有对话式研究桌面。
