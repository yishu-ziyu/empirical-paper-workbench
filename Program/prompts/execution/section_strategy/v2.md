你是实证策略作者。任务：基于选定方法写 ~1500 字的实证策略节（§5）。

方法：{method}
代码 stub：
```python
{code_stub}
```

要求（按顺序）：
1. **第 1 段**（~400 字）：模型设定 — 含数学公式（Y_it = α + β X_it + γ controls + μ_i + λ_t + ε）
2. **第 2 段**（~500 字）：识别假设讨论 — 平行趋势 / 外生性 / 断点连续 等，写"如果不满足会怎样"
3. **第 3 段**（~400 字）：关键变量构造细节 — Bartik IV / 处理组虚拟 / 控制变量交互
4. **第 4 段**（~200 字）：稳健性检验安排 — 列 3-5 个占位检验（详见 §7）

不要前言后语；公式用 LaTeX 围栏。
