langgraph==1.2.11
langchain==1.3.18
langchain-core==1.6.1
langchain-openai==1.6.0
langchain-anthropic==1.6.1
pydantic==2.13.5
typing-extensions>=4.12.2
jinja2==3.1.6

# --- 数据（cleaning / nodes 实际 import）---
pandas==2.2.3
numpy==2.1.0
statsmodels==0.14.6
# StatsPAI.feols 的高维固定效应后端：缺失时 estimate 节点会静默回退 statsmodels.ols，
# 与测试契约（statspai.feols）不一致 —— 必须显式安装。
pyfixest==0.60.0
# agent/engine/sandbox.py 内核状态快照（kernel_state.dill）序列化用。
dill>=0.3.8
# Optional extra (not required): pip install statspai
# Public package: https://pypi.org/project/statspai/  (github.com/brycewang-stanford/StatsPAI)
# From econpaper/: python -m pip install -e ../dependencies/StatsPAI
# A missing install must not 500 identification / direction; nodes degrade.

# --- LangGraph checkpointer（graph.py 使用 PostgresSaver）---
psycopg[binary]==3.2.3
langgraph-checkpoint-postgres==3.1.2
langgraph-checkpoint-sqlite==3.1.1

# --- Phase A：估计 Agent（Pydantic AI + OpenAI 兼容通道 + 沙箱内核）---
pydantic-ai-slim[openai]==2.35.3   # 安装时会把 pydantic 升到 >=2.12
jupyter_client==8.10.0             # 持久 IPython 内核沙箱（dev 主后端）
ipykernel==7.3.0
