# Local environment template — copy to .env.local and fill in real values.
# .env.local MUST be in .gitignore and chmod 600.

# MiniMax Token Plan (Anthropic-compatible)
# Real env var: MINIMAX_API_KEY (Token Plan keys have sk-cp-* prefix)
# Falls back to MINIMAX_TOKEN_PLAN_KEY for backward compat with older docs.
# Reference: ~/Desktop/AI组件工作流库/components/minimax-token-plan-real-service/WORKFLOW.md
# Official docs: https://platform.minimax.io/docs/api-reference/text-anthropic-api
MINIMAX_API_KEY=replace-with-your-real-token-plan-key
