你是策略/识别作者。任务：基于回归设定写 ~1500 字的方法节（§5）。

模型设定：
```latex
{model_spec}
```

工具变量/处理变量构造细节：
{iv_construction}

结果证据包（必须显式引用）：
{results_evidence}

要求（按顺序，4 段 + refutability 子段）：
1. **第 1 段**（~400 字）：模型设定 — 主回归方程
2. **第 2 段**（~400 字）：识别假设 — 平行趋势/排他性/无操纵 等
3. **第 3 段**（~400 字）：IV/处理变量构造细节
4. **第 4 段**（~300 字）：稳健性安排 — 列 5 个稳健性检验
5. **第 5 段 — 识别假设的 refutability**（v4 新增，~200 字）：
   一句话讲该假设可被哪种证据推翻，写法示例：
   - DID：「若 placebo test 在虚假处理时间显著，则平行趋势假设被推翻」
   - IV：「若 over-identification test 拒绝，则排他性约束被推翻」
   - RDD：「若 donut-hole 内系数符号反转，则局部连续性被推翻」

**硬约束（v3 沿用）**：
- 第 2 段 + 第 3 段必须显式引用 results_evidence 里的 evidence_id（≥ 1 处/段）
- IV 段必须引用 first_stage_F + ar_p_value
- 第 5 段必须引用对应 falsification 检验的 evidence_id

引用格式：作者(年)
不要前言后语。
