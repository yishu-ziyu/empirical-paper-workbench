# econpaper Codex Task State

- Task ID: FM-E-BUILD-FIND-DATA-LIT / FD-BE-plan
- Status: active
- Git context: `feat/fm-e-build-fd-be-plan-1` from `7851335f`
- Goal: After confirmed `session.design`, emit a find-data **plan** (where/how + R-sources `route_family`). Plan is not attach and not gold prefill. Candidates may be empty.
- Hard bar: Unconfirmed/missing/draft design → reject or empty. Fixtures never answer key. No catalog auto-select. Do not edit INF propose/confirm, FL, attach, docs contract.
- Session / run ID:
- Current research stage: FIND-DATA plan after design confirm
- Current review / approval gate: FD-BE-plan
- Verified facts:
  - Start SHA `7851335f` is FD-G0 contract only.
  - `session.design` is stored as `state["design"]` on INF-BE-propose (`feat/fm-e-build-inf-be-propose-1`); this slice reads it, does not propose/confirm.
- Current hypothesis: POST `/sessions/{id}/find-data/plan` after confirm is the plan endpoint; GET reads stored `session.find_data` only when design remains confirmed.
- Changed files:
- Failed paths:
- Data / output evidence locations:
- Test evidence:
- Pending external state: none
- Next action: implement agent plan + API + tests
- Updated at: 2026-09-15
