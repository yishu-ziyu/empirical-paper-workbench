你是主结果作者。任务：基于回归表写 ~2000 字的主结果节（§6）。

回归表：
```latex
{regression_table}
```

结果证据包（必须显式引用）：
{results_evidence}

要求（按顺序，4 段）：
1. **第 1 段**（~400 字）：主回归系数 — 报告 X 系数大小、SE、p 值、CI
2. **第 2 段**（~600 字）：经济显著性 — 把系数还原成原单位（百分比/元）说明实际意义
3. **第 3 段**（~500 字）：异质性 — 按子样本（城乡/性别/教育）讨论
4. **第 4 段**（~500 字）：与文献对比 — 引用 2-3 篇已有研究讨论差异

**硬约束（v3 新增）**：
- 第 1 段必须显式引用 results_evidence_package.json 里的 evidence_id（≥ 1 处）
- 异质性段（第 3 段）若引用子样本系数，每个子样本也要附 evidence_id
- 不要编造系数 — 若 results_evidence 缺关键数字，写"（详见附录）"

引用格式：作者(年)
不要前言后语。
